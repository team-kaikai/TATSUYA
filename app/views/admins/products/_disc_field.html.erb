<div class="nested-fields">
	<div class="songs">
	    <%= f.fields :songs do |song| %>
		    <%= render 'song_fields', f:song %>
	    <% end %>
	    <div class="links">
		    <%= link_to_add_association "add songs", f, :songs %>
	    </div>
	</div>
</div>